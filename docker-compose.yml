version: '3.8'
services:
  python-app:
    build:
      context: ./python-app
      args:
        VARIANT: 3.10-bullseye
        NODE_VERSION: lts/*
  node-app:
    build:
      context: ./node-app
      args:
        VARIANT: 16-bullseye
